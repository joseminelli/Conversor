{
  "buildCommand": "cd api && pip install -r requirements.txt",
  "outputDirectory": "api",
  "framework": "python",
  "functions": {
    "api/**": {
      "runtime": "python3.11"
    }
  },
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "api/main.py"
    }
  ]
}
